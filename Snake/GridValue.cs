
namespace Snake
{
     public enum GridValue 
    {
       //Decalring Values for Grid
        Empty, // This is the empty values in the grid
        Snake, // This is the value(s) that the snake holds
        Food, // This is the value where the food will spawn
        Outside, // This enum variable wont be included in our array 
    }
}
